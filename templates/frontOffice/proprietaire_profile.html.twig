{% extends 'frontOffice/base.html.twig' %}

{% block title %}Mon Profil - Stayzy{% endblock %}

{% block body %}
<main class="main">
    <section style="padding: 80px 0; background: #f8f9fa; min-height: 80vh;">
        <div class="container">
            
            {# Messages flash #}
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show">
                    <i class="bi bi-check-circle me-2"></i>{{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            <div class="row">
                
                {# COLONNE GAUCHE - Carte profil #}
                <div class="col-lg-4">
                    <div class="card border-0 shadow-sm" style="border-radius: 16px; position: sticky; top: 100px;">
                        <div class="card-body text-center" style="padding: 40px 30px;">
                            
                            {# Photo de profil #}
                            <div style="position: relative; display: inline-block;">
                                {% if user.profilePicture %}
                                    <img src="{{ asset('uploads/profiles/' ~ user.profilePicture) }}" 
                                         alt="Photo de profil"
                                         style="width: 150px; height: 150px; border-radius: 50%; object-fit: cover; border: 5px solid #2e7d32;">
                                {% else %}
                                    <div style="width: 150px; height: 150px; border-radius: 50%; background: #2e7d32; display: flex; align-items: center; justify-content: center; font-size: 60px; color: white; font-weight: 700;">
                                        {{ user.prenom|slice(0,1)|upper }}{{ user.nom|slice(0,1)|upper }}
                                    </div>
                                {% endif %}
                                
                                {# Badge statut #}
                                <span style="position: absolute; bottom: 10px; right: 10px; background: {% if user.approvalStatus == 'approved' %}#2e7d32{% elseif user.approvalStatus == 'pending' %}#f57f17{% else %}#c62828{% endif %}; color: white; padding: 5px 12px; border-radius: 20px; font-size: 12px; font-weight: 600;">
                                    {% if user.approvalStatus == 'approved' %}
                                        ✅ Vérifié
                                    {% elseif user.approvalStatus == 'pending' %}
                                        ⏳ En attente
                                    {% else %}
                                        ❌ Rejeté
                                    {% endif %}
                                </span>
                            </div>

                            {# Nom #}
                            <h4 class="mt-3 mb-1" style="font-weight: 700; color: #1a1a1a;">
                                {{ user.prenom }} {{ user.nom }}
                            </h4>
                            <p class="text-muted mb-3" style="font-size: 14px;">
                                <i class="bi bi-envelope me-1"></i> {{ user.email }}
                            </p>

                            {# Badge propriétaire #}
                            <span style="background: #fff8e1; color: #f57f17; padding: 6px 16px; border-radius: 20px; font-size: 13px; font-weight: 600;">
                                <i class="bi bi-building me-1"></i> Propriétaire
                            </span>

                            <hr style="margin: 25px 0;">

                            {# Statistiques rapides #}
                            <div class="row text-center">
                                <div class="col-6 mb-3">
                                    <div style="background: #e8f5e9; padding: 15px; border-radius: 10px;">
                                        <h3 style="color: #2e7d32; font-weight: 700; margin: 0;">0</h3>
                                        <small class="text-muted">Propriétés</small>
                                    </div>
                                </div>
                                <div class="col-6 mb-3">
                                    <div style="background: #e3f2fd; padding: 15px; border-radius: 10px;">
                                        <h3 style="color: #1565c0; font-weight: 700; margin: 0;">0</h3>
                                        <small class="text-muted">Réservations</small>
                                    </div>
                                </div>
                            </div>

                            <hr style="margin: 25px 0;">

                            {# Score de Confiance #}
                            {% include 'components/trust_score.html.twig' %}

                            {# Bouton déconnexion #}
                            <a href="{{ path('app_logout') }}" class="btn btn-outline-danger w-100 mt-3" style="border-radius: 10px;">
                                <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                            </a>
                        </div>
                    </div>
                </div>

                {# COLONNE DROITE - Formulaire #}
                <div class="col-lg-8">
                    <div class="card border-0 shadow-sm" style="border-radius: 16px;">
                        <div class="card-header" style="background: white; border-bottom: 2px solid #f0f0f0; padding: 25px 30px;">
                            <h5 style="font-weight: 700; color: #1a1a1a; margin: 0;">
                                <i class="bi bi-person-gear me-2" style="color: #2e7d32;"></i> Modifier mon profil
                            </h5>
                        </div>
                        <div class="card-body" style="padding: 30px;">
                            
                            {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}

                            {# Upload photo #}
                            <div class="mb-4">
                                <label class="form-label fw-semibold">
                                    <i class="bi bi-camera me-1" style="color: #2e7d32;"></i> Photo de profil
                                </label>
                                <div class="d-flex align-items-center gap-3">
                                    <div>
                                        {% if user.profilePicture %}
                                            <img src="{{ asset('uploads/profiles/' ~ user.profilePicture) }}" 
                                                 alt="Aperçu"
                                                 id="preview"
                                                 style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; border: 3px solid #2e7d32;">
                                        {% else %}
                                            <div id="preview" style="width: 80px; height: 80px; border-radius: 50%; background: #e0e0e0; display: flex; align-items: center; justify-content: center; font-size: 30px; color: #999;">
                                                <i class="bi bi-person"></i>
                                            </div>
                                        {% endif %}
                                    </div>
                                    <div style="flex: 1;">
                                        <input type="file" 
                                               name="profile_picture" 
                                               id="profilePicture"
                                               class="form-control"
                                               accept="image/*"
                                               onchange="previewImage(event)"
                                               style="border-radius: 10px;">
                                        <small class="text-muted">JPG, PNG ou GIF - Max 2MB</small>
                                    </div>
                                </div>
                            </div>

                            <hr>

                            {# Informations personnelles #}
                            <h6 class="mb-3" style="color: #2e7d32; font-weight: 600;">
                                <i class="bi bi-person-badge me-1"></i> Informations personnelles
                            </h6>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    {{ form_row(form.prenom, {
                                        'label': 'Prénom',
                                        'attr': {'class': 'form-control', 'style': 'border-radius: 10px; padding: 12px;'}
                                    }) }}
                                </div>
                                <div class="col-md-6 mb-3">
                                    {{ form_row(form.nom, {
                                        'label': 'Nom',
                                        'attr': {'class': 'form-control', 'style': 'border-radius: 10px; padding: 12px;'}
                                    }) }}
                                </div>
                            </div>

                            <div class="mb-3">
                                {{ form_row(form.email, {
                                    'label': 'Email',
                                    'attr': {'class': 'form-control', 'style': 'border-radius: 10px; padding: 12px;'}
                                }) }}
                            </div>

                            <div class="mb-3">
                                {{ form_row(form.tel, {
                                    'label': 'Téléphone',
                                    'attr': {'class': 'form-control', 'style': 'border-radius: 10px; padding: 12px;', 'placeholder': '+216 XX XXX XXX'}
                                }) }}
                            </div>

                            <hr class="my-4">

                            {# Changer mot de passe #}
                            <h6 class="mb-3" style="color: #2e7d32; font-weight: 600;">
                                <i class="bi bi-key me-1"></i> Changer le mot de passe
                            </h6>

                            <div class="mb-3">
                                {{ form_row(form.password, {
                                    'label': 'Nouveau mot de passe (optionnel)',
                                    'attr': {'class': 'form-control', 'style': 'border-radius: 10px; padding: 12px;', 'placeholder': 'Laissez vide pour ne pas changer'}
                                }) }}
                                <small class="text-muted">Laissez vide si vous ne voulez pas changer votre mot de passe</small>
                            </div>

                            {# Boutons #}
                            <div class="d-flex gap-2 mt-4">
                                <button type="submit" class="btn btn-success btn-lg" style="border-radius: 10px; flex: 1;">
                                    <i class="bi bi-check-circle me-2"></i> Enregistrer les modifications
                                </button>
                                <a href="{{ path('app_home') }}" class="btn btn-outline-secondary btn-lg" style="border-radius: 10px;">
                                    <i class="bi bi-x-circle"></i> Annuler
                                </a>
                            </div>

                            {{ form_end(form) }}

                        </div>
                    </div>

                    {# Suggestions IA #}
                    {% include 'components/trust_suggestions.html.twig' %}

                    {# Section propriétés (pour plus tard) #}
                    <div class="card border-0 shadow-sm mt-4" style="border-radius: 16px;">
                        <div class="card-header" style="background: white; border-bottom: 2px solid #f0f0f0; padding: 25px 30px;">
                            <h5 style="font-weight: 700; color: #1a1a1a; margin: 0;">
                                <i class="bi bi-house-door me-2" style="color: #2e7d32;"></i> Mes propriétés
                            </h5>
                        </div>
                        <div class="card-body text-center" style="padding: 60px 30px;">
                            <i class="bi bi-house-add" style="font-size: 60px; color: #ddd;"></i>
                            <h6 class="mt-3 text-muted">Vous n'avez pas encore de propriétés</h6>
                            <p class="text-muted mb-4">Ajoutez votre première propriété pour commencer à recevoir des réservations!</p>
                            <button class="btn btn-success" style="border-radius: 10px;">
                                <i class="bi bi-plus-circle me-2"></i> Ajouter une propriété
                            </button>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </section>
</main>

<script>
function previewImage(event) {
    const preview = document.getElementById('preview');
    const file = event.target.files[0];
    
    if (file) {
        const reader = new FileReader();
        reader.onload = function(e) {
            preview.innerHTML = '<img src="' + e.target.result + '" style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; border: 3px solid #2e7d32;">';
        }
        reader.readAsDataURL(file);
    }
}
</script>
{% endblock %}